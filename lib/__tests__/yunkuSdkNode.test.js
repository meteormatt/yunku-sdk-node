const assert = require('assert');
const yunkuSdkNode = require('../index.js');

describe('yunkuSdkNode', () => {
  it('has a test', () => {
    assert(true, 'yunkuSdkNode should have a test');
  });
});
